# Current State

The active question is whether reranking helps after the latest data fix.

Current evidence is inconclusive because A8 changed reranking while the project
also carries an older stale note from before the data fix.

Next contrast: run retrieval-only and retrieval-plus-reranking on the same data
snapshot, same evaluation set, and same scoring script.

