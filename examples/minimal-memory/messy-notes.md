# Messy Notes

A7 looked better than baseline on retrieval quality. A8 failed after adding
reranking, but there was also a data fix between the older reranking note and
this run, so the old note may not apply.

Need next experiment. Maybe test retrieval only against retrieval plus reranking
on the same data snapshot.

